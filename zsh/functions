function new-tmux-from-dir-name {
  tmux new-session -As `basename $PWD`
}

function tj {
  tmux attach-session -t `tmux list-sessions | sed -E 's/:.*$//' | selecta`
}


function aoj-downloader() {
  youtube-dl -o "~/Desktop/aoj/%(title)s" "https://players.brightcove.net/4298059952001/default_default/index.html?videoId=$1" --referer "https://www.artofjiujitsu.com"
}

function aoj() {
  for var in "$@"
    do
      aoj-downloader $var
    done
}
